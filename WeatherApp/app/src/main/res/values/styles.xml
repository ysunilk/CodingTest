<resources>

    <style name="textSmallWeather" parent="@android:style/TextAppearance.Small">
        <item name="android:textSize">@dimen/textSmallWeather.size</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
